/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        hostname: "4c1n8w1exu.ufs.sh",
        protocol: "https",
        port: "",
      },
    ],
  },
};

export default nextConfig;
